import styled from "styled-components"
import{ colors } from "../../styles/GlobalStyle"


export const ButtonN = styled.footer`
   cursor: pointer;

height: 5rem;
width: 14rem;
button{
    background-color: ${colors.branco};
    font-size: 1.8rem;
    font-weight: bold;
    cursor: pointer;
}
background-color: ${colors.branco};
border-radius: 1rem;
`
