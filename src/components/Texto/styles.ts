import styled from "styled-components"
import{ colors } from "../../styles/GlobalStyle"


export const Texto = styled.header`
  /* Rodapé */
    background-color: ${colors.primary};
    height: 4rem;
    width: 100%;
    display: flex;
   
p {
    background-color:${colors.third} ;
}


`

